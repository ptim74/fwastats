@model FWAStatsWeb.Models.PlayerViewModels.UnlinkViewModel
@{
    ViewData["Title"] = "Unlink Player";
}

<div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
    <div class="panel panel-info">
        <div class="panel-heading">
            <div class="panel-title">@ViewData["Title"]</div>
        </div>
        <div class="panel-body">

            <form asp-controller="Players" asp-action="DeleteLink" method="post" class="form-horizontal">

                <div asp-validation-summary="All" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="Tag" class="col-md-3 control-label"></label>
                    <div class="col-md-9">
                        <input asp-for="Tag" class="form-control" autocomplete="off" />
                        <span asp-validation-for="Tag" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-3 col-md-9">
                        <button type="submit" class="btn btn-danger">Unlink</button>
                    </div>
                </div>

            </form>

        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}